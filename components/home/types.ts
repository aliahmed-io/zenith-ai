import { ElementType } from "react";

export interface Feature {
  icon: ElementType;
  title: string;
  description: string;
  link: string;
}

export interface EnhancedFeature {
  icon: ElementType;
  title: string;
  description: string;
}
